# OpenRouter / Exa 真实联调记录

日期：2026-09-12。模型为 `deepseek/deepseek-v4.1-flash`，通过 OpenRouter 官方 AI SDK provider 2.10.0 调用；搜索使用 Exa Python SDK 2.14.0。密钥仅存本机受限 `.env`，不纳入 Git。

## 本轮通过的真实场景

| 场景 | 单次耗时 | 实际工具 |
| --- | --- | --- |
| 读取当天餐食、训练事实及原请求重放 | 15.44 秒 | context |
| 选中餐食改为半份、重读总量、显式 Undo | 34.08 秒 | context, meal_update, context |
| 检索 Pret A Manger Hong Kong 菜单与来源 | 35.29 秒 | context, menu_search |
| 上传测试菜单图片，读取两道菜及 HK$58 / HK$42 | 26.95 秒 | context, context |

餐食修改确实将 consumedFraction 从 1 改成 0.5，重新计算摄入，Undo 后恢复到 1；没有仅根据助手文字判断成功。搜索确实调用 Exa 并返回来源；图片读取没有调用菜单搜索或修改餐食。测试只使用隔离的演示数据、临时 PostgreSQL 和自行制作的菜单测试卡，没有使用真实用户健康数据。

## 联调发现并修复

- 原先默认高强度推理与 20 秒运行限制不适合实际请求。使用官方 OpenRouter provider，显式关闭推理，单次最多 4096 输出 tokens，Node 全轮 115 秒、FastAPI 租约 120 秒，并预留终结回执时间。
- 该模型在工具循环每一步同时施加 JSON response_format 时出现直接给出引言、承诺搜索、甚至声称修改而未调用工具的情况。工具循环改用原生 tool calling，最终 JSON 由 Zod 和 FastAPI 严格校验；模型自行选工具的菜单搜索实测通过。
- 可信用户意图及选中餐食目标传入模型上下文。明确且完整的修改、撤销、训练进度意图先要求实际工具调用；FastAPI 拒绝未尝试对应操作就直接完成，避免“已修改”而实际未保存。
- 聊天正文须独立完整，不把菜名价格等答案只放进 Today 摘要。仅省略号的占位回复不会被保存为完成。
- 即使上游忽略 abort，HTTP SSE 也按本地截止时间结束；迟到结果不能写入。新增离线回归覆盖不合作的上游、剩余租约与占位答案。

## 回归与边界

后端 pytest 248 项、Node 38 项通过；Node 构建及依赖审计通过。受控全链路覆盖会话隔离、重放、取消、写后重读、Undo、重启；真实调用只验证上表场景。

耗时是本轮单次样本，不是性能保证。调试期间出现过超时和一次上游流中断，不能据此声称长时间稳定性或所有自然语言输入都已验收。原文授权仍使用有限的明确意图语法，复杂复合表达可能需要澄清。模型仍可能附带未请求的建议，专业依据与内容质量须在 RAG 联调中进一步验收。测试菜单卡不代表复杂食物照片、份量估算均已通过。

本次未包含知识库 RAG、实际演示电脑验收或 macOS 安装包。普通 pytest 不读取真实凭据；真实测试必须显式使用 `scripts/live_smoke.py --live`。
